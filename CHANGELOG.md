# Changelog

이 프로젝트의 주요 변경사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며, 버전은 [SemVer](https://semver.org/lang/ko/)를 지향합니다.

> 이 리포지토리는 **폴리글랏 SDK**입니다. Java(`io.github.xzawed:keycloak-sdk`)·Python(`keycloak-sdk`)·Node(`@xzawed/keycloak-sdk`)·Go(`github.com/xzawed/KeyCloakSDK/go`)·C#/.NET(`Xzawed.Keycloak.Sdk`)·PHP(`xzawed/keycloak-sdk`)·Rust(`keycloak-sdk`)·Ruby(`keycloak-sdk`)·Kotlin(`io.github.xzawed:keycloak-sdk-kotlin`) 9개 언어가 독립 배포되며, 아래 항목은 언어 태그로 구분합니다. 지금까지 여섯 언어가 **첫 릴리스 후보(RC)**를 게시했고(PHP `v0.1.0-rc.1` · Python `0.1.0rc1` · .NET `0.1.0-rc.1` · Rust `0.1.0-rc.1` · Ruby `0.1.0.rc1` · Node `0.1.0-rc.2`), 나머지 셋(Java·Go·Kotlin)은 미게시입니다(Java는 `v0.1.0-RC1`이 Central Portal 스테이징까지 올라가 수동 Publish 대기) — 정식(stable) 릴리스는 아직 어느 언어도 없으므로 아래 항목은 모두 `[Unreleased]`입니다.

## [Unreleased]

### Fixed
- **(Python) `AsyncAdminClient.aclose()`가 admin 토큰그랜트의 httpx 클라이언트를 닫지 않아 FD가 누수됐다.** admin에는 async 클라이언트가 **둘**(admin REST · 지연 생성되는 client-credentials 토큰그랜트)인데 바깥 하나만 닫고 있었다 — 게시된 `0.1.0rc1`을 실 Keycloak에 대해 돌려 실측했다: `aclose()` 후 안쪽이 `is_closed=False`로 남고 "unclosed transport"/"unclosed socket" ResourceWarning과 함께 admin을 쓰는 클라이언트마다 소켓 FD가 하나씩 샜다(장기 async 서비스에서 EMFILE). auth 전용 클라이언트는 영향 없다. 지금은 둘 다 닫으며, 중첩 정리가 실패해도 `finally`로 바깥 FD는 회수한다(실패 자체는 전파). 같은 이중 구조를 이미 알고 있던 리다이렉트 하드닝(`_internal/redirects.py`)과 대칭을 이룬다. 수정본 실측: `nested_closed=True`, ResourceWarning 0건. (2026-08-03)
- **(Python) `keycloak_sdk.__version__`이 매니페스트와 어긋났다 — 게시된 휠이 자신을 `0.1.0`으로 보고했다.** `__init__.py`에 하드코딩된 상수가 `pyproject.toml`의 `0.1.0rc1` 범프를 따라가지 못했고, 스모크 테스트가 같은 상수를 비교하고 있어 초록이었다. 이제 `importlib.metadata`에서 파생하고(미설치 소스트리는 `0.0.0+source` 폴백), 테스트는 배포 메타데이터와의 일치를 고정한다. (2026-08-03)

### Added
- **(harness) 8개 언어 종합 검증·점수책정 하네스 — `main` 병합 (PR #20).** 기존 5언어 k6 하네스를 8개 언어(Go·C#·Node·Python·Java·PHP·Rust·Ruby)로 확장하고, 그 위에 심화 검증 4종 + 스코어링 레이어를 얹었다. 8개 샘플 앱이 각 언어 관용 프레임워크(net/http·ASP.NET Core·Express 5·FastAPI·Spring Boot·Slim 4·axum·Rack/Puma)로 동일 [HTTP 계약 v2](harness/contract/CONTRACT.md)(auth 확장 4엔드포인트 + admin 5리소스 CRUD + 오류경로)를 구현해 실제 Keycloak 26.6을 소비한다. 신규 오케스트레이터 `verify.sh`가 언어별로 **conformance**(`conformance/conformance.mjs`, 계약 준수 assert) → **security**(`security/probe.mjs`, JWT 하드닝 공격 프로브 — alg=none·HS/RS confusion·미지kid·flood 등) → k6 성능을 실행하고, 전 언어 종료 후 **suites**(`suites/run-suite.sh`, 각 SDK 자체 단위테스트+커버리지+린트를 툴체인 이미지에서 실행)를 집계, `report/score.mjs`가 **4차원 가중 스코어카드**(기능 30%·보안 30%·커버리지 20%·성능/동형성 20%, 등급 A≥90/B≥80/C≥70/D<70)를 `report/SCORECARD.md`로 산출한다(신호는 `report/signals/<lang>.{conformance,security,suite}.json`). TDD·WBS 기반 subagent-driven 실행(태스크별 fresh 구현자 + 리뷰 게이트 + opus 최종 whole-branch 리뷰 READY). app-go 검증 실측: conformance 26/26·security 9/9 defended. CI(`harness.yml`)는 PR/푸시엔 빠른 Go 스모크(`mvp-go`), 야간(schedule 03:00 UTC)·수동엔 5언어 k6 비교(`all-langs`) + 8언어 스코어링(`score-all`, `SCORECARD.md`+`signals/` 아티팩트)을 실행. ⚠️ 성능·동형성 차원은 k6 실측 미연동(conformance 통과율 근사, 후속). 앱 빌드 이미지는 Alpine(musl) 베이스(Docker Desktop/Windows glibc-DNS 게차 회피). (2026-07-07)
- **(Ruby) `keycloak-sdk`(gem) 8번째(마지막) 언어 SDK 추가 — Ruby(sync-only).** 성숙한 admin gem 부재로 `faraday` ~>2.0로 Admin REST 직접 래핑(5리소스+raw) + `rack-oauth2` ~>2.3 래핑(인증, PKCE S256은 손수 생성) + `jwt`(ruby-jwt) ~>3.2 자체 강화 JWT 검증. Java/Python/Node/C#/PHP와 §4 동형(Ruby 관용: `Data.define` 값타입 + `inspect` 마스킹, 예외 계급 `KeycloakSdk::Error`→`ConfigError`/`AuthError`/`TransportError`/`TokenValidationError`/`AdminError`→`NotFoundError`/`ConflictError`/`ForbiddenError`): `KeycloakClient`(auth 즉시·admin 지연+전용 캐싱 provider·`close`), `AuthClient`(PKCE·client-credentials·exchange_code·refresh·introspect·logout·validate), `AdminClient`(Users/Clients/Realms/Roles/Groups + `raw` + 오류 경계변환), `TokenProvider` 덕 인터페이스(Mutex 캐시·single-flight)·`JwksStore`(DoS-안전). JWT 강화(alg 핀 RS256·`none` 거부·iss/aud/exp/nbf·클록 스큐·nil iss/aud fail-closed). 단위 73 + 통합 1(E2E `full_flow`, docker CLI 셸아웃, 실제 Keycloak 26.6) = **총 74 GREEN**, 라인 100%/브랜치 93.48%(게이트 90/85, 네트워크 경계 omit), `rubocop` 무경고·`bundler-audit` 통과. 착수 전 딥리서치로 라이브러리 API 확정, 완료 후 opus 어드버서리얼 리뷰로 확정 결함 수정(JwksStore nil-캐시 rate-limit·rack-oauth2 API 관용 — `http_config`/`e.response[:error]`/id_token 추출/scope kwarg·Faraday::SSLError 형제 경계). RubyGems Trusted Publishing(OIDC) 릴리스 CI 준비(human-gated), 태그 `ruby-v*`. 최소 Ruby 3.2. (2026-07-06)
- **(Rust) `keycloak-sdk`(crate) 7번째 언어 SDK 추가 — Rust 1.88+(edition 2024)·async-only(tokio).** `keycloak` crate =26.6.2 래핑(Admin, `reqwest12` feature로 reqwest 0.12 정렬) + `openidconnect` =4.0.1 래핑(인증, 수동 `EndpointSet` typestate·PKCE S256) + `jsonwebtoken` =10.4.0 자체 강화 JWT 검증. Java/Python/Node/Go/C#/PHP와 §4 동형(Rust 관용: `thiserror` 기반 `KeycloakError` enum·수동 Debug 마스킹·`Result<T, KeycloakError>`): `KeycloakClient`(공유 `reqwest::Client`·SSRF redirect none·auth 즉시·admin 주입 캐싱 provider), `AuthClient`·`AdminClient`(5리소스+`raw()`)·`TokenProvider` trait(async)·`JwksStore`(DoS-안전 rate-limit 결정시점 stamp). JWT 강화(`Validation` 기본값 전부 재정의 — alg 핀 RS256·`validate_nbf`·`leeway`·`required_spec_claims` exp/iss/aud). 단위 34 + Testcontainers 통합 1(E2E `full_flow`, 실제 Keycloak 26.6) = **총 35 GREEN**, 로직 라인 커버리지 94.85%(게이트 ≥90, 네트워크 경계 omit), `cargo clippy -D warnings`(0 경고)·`cargo fmt --check` 통과. 착수 전 딥리서치로 라이브러리 API 확정(reqwest 메이저 정렬·typestate 제네릭·`Validation` 안전기본값 부재), 완료 후 opus 최종리뷰로 확정 결함 수정(admin이 캐싱 `ClientCredentialsTokenProvider`를 쓰도록 재배선해 §4 캐시/single-flight 불변식 복원·`config.scopes` threading). crates.io 배포 CI(`cargo publish`) 준비(human-gated), 태그 `rust-v*`. 최소 Rust 1.88(edition 2024 + let-chain). (2026-07-06)
- **(PHP) `xzawed/keycloak-sdk` 6번째 언어 SDK 추가 — PHP 8.3+(`final readonly class` 값타입·예외 기반).** `fschmtt/keycloak-rest-api-client-php` 0.42.0 래핑(Admin) + `league/oauth2-client`+`stevenmaguire/oauth2-keycloak` 래핑(인증, PKCE S256 오버라이드) + `firebase/php-jwt` ^7.1 자체 강화 JWT 검증. Java/Python/Node/Go/C#와 §4 동형(PHP 관용: `final readonly class` + `__toString` 마스킹, 예외 계급 `KeycloakException`→`Config`/`Auth`/`Transport`/`TokenValidation`/`Admin`→`NotFound`/`Conflict`/`Forbidden`): `KeycloakClient`(auth 즉시·admin 지연캐시·`close`), `AuthClient`(+`Internal/PkceKeycloakProvider` S256), `AdminClient`(Users/Clients/Realms/Roles/Groups + `raw()` + `ErrorTranslation` 경계변환)·`TokenProvider`·`JwksStore`(DoS-안전). JWT 강화(첫 세그먼트 직접 디코드로 alg 사전 게이트·`none` 거부·iss 정확일치·aud 포함검사·`exp` 필수·클록 스큐·악성 JWKS `\TypeError` 경계 전면화). 단위 64 + 통합 3(`FullFlowIT`, docker CLI 셸아웃, 실제 Keycloak 26.6) = **총 67 GREEN**, 집계 로직 라인 커버리지 100.00%(게이트 ≥90, 네트워크 경계 omit), `phpstan`(level max)·`php-cs-fixer`·`composer audit` 통과. 착수 전 딥리서치로 라이브러리 관용 확정(`Users::create()` void 반환·`Clients`/`Realms`는 `import`·`pkceMethod` 옵션 no-op·`&$headers`는 성공 디코드 후에만), Task 7(JwtValidator) OPUS 어드버서리얼 보안리뷰로 Critical 1건(악성 JWKS `\TypeError` 누출) 수정. Packagist(GitHub 웹훅 자동게시) 릴리스 CI 준비(human-gated), 태그 `php-v*`. 최소 PHP 8.3. (2026-07-06)
- **(dotnet) `Xzawed.Keycloak.Sdk` 5번째 언어 추가 — C#/.NET(async-first, `Task<T>`+`CancellationToken`).** `Duende.IdentityModel` 8.1.0(인증) + `Microsoft.IdentityModel.JsonWebTokens`/`.Protocols.OpenIdConnect` 8.19.1(자체 강화 JWT) + `Keycloak.AuthServices.Sdk` 2.7.0(Admin, net8 최종 빌드 — 3.0.0은 net10 전용) 래핑. Java/Python/Node/Go와 §4 동형(C# 관용: `record` 값타입 + `ToString()`/`JsonConverter<T>` 이중 마스킹, 예외 계급 `KeycloakException`→`KeycloakAdminException`(`StatusCode`)→`KeycloakNotFoundException`/`KeycloakConflictException`/`KeycloakForbiddenException`, `KeycloakTransportException`): `KeycloakClient.Create`(Auth 즉시·`AdminAsync()` 지연 single-flight·`IAsyncDisposable`+`IDisposable`), `AuthClient : ITokenSource`(PKCE·client-credentials·ExchangeCode+nonce fail-closed·Refresh·Introspect·Logout·Validate), `AdminClient`(Users/Groups/Realms 타입드 + Clients/Roles raw REST + `Raw` + 타임아웃 주입 + 오류 경계변환), `TokenSet`/`ValidatedToken`/`IntrospectionResult`/`AuthorizationRequest`·`ITokenProvider`(SemaphoreSlim single-flight)·`AddKeycloak` DI 확장. JWT 강화(alg 핀 `RS256`·`none` 거부·iss 정확일치·aud 포함검사·`exp` 필수·클록 스큐 30s·DoS-안전 JWKS `ConfigurationManager.RefreshInterval`). 단위 58 + Testcontainers 통합 1(`Full_flow` E2E, 실제 Keycloak 26.6, 전 흐름·5 admin 리소스) = **총 59 GREEN**, 로직 커버리지 라인 97.64%/브랜치 93.18%(게이트 90/85, 네트워크 경계 `AuthClient`/`Admin.*`/`KeycloakClient` omit), `dotnet build`(warnaserror)·`dotnet format` 통과. 착수 전 딥리서치로 라이브러리 API 확정(admin 타입드 커버리지가 users/groups/realm-get뿐이라 clients/roles/realm-CRUD는 raw REST, `ValidateTokenAsync`가 예외 미던짐 등), 완료 후 실제 컴파일 포함 다중에이전트 어드버서리얼 검증으로 확정 결함 보정(네임스페이스 셰도잉 별칭·테스트 프로젝트 CS1591 게이트·exp-필수 테스트 무효화·JSON 마스킹·nonce fail-open·release workflow env 스코프). `NUGET_API_KEY` 시크릿 기반 NuGet 릴리스 CI 준비(human-gated, 미실행), 태그 `dotnet-v*`. 최소 .NET 8(net8.0). (2026-07-05)
- **(Go) `github.com/xzawed/KeyCloakSDK/go` 4번째 언어 SDK 추가 — Go(sync + `context.Context`).** `Nerzal/gocloak` v13.9(Admin) + `golang.org/x/oauth2` v0.36(인증) 래핑 + `go-jose/v4` v4.1.4 자체 강화 JWT 검증. Java/Python/Node와 §4 동형(Go 관용: no-stutter 명명 `keycloak.New`/`Client`/`Config`, 타입드 구조체 + 센티넬 오류 `errors.Is`/`errors.As`, `context.Context`): `keycloak.Client`(Auth 즉시·`Admin(ctx)` 지연·`Close`), `AuthClient`(PKCE·client-credentials·ExchangeCode·Refresh·Introspect·Logout·Validate), `AdminClient`(Users/Clients/Realms/Roles/Groups + `Raw()` + 타임아웃 주입 + 오류 경계변환), `TokenSet`/`ValidatedToken`/`IntrospectionResult`·오류 계급·센티넬 `ErrNotFound`/`ErrConflict`/`ErrForbidden`·`TokenProvider`. JWT 강화(alg 핀·`none` 거부·iss 정확일치·aud 포함검사·`exp` 필수·클록 스큐·DoS-안전 JWKS single-flight+rate-limit). 단위 40 + Testcontainers 통합 1(E2E, 실제 Keycloak 26.6, 전 흐름·5 admin 리소스) = **총 41 GREEN**, 로직 커버리지 95.2%(네트워크 경계 omit), `go vet`·`gofmt`·`golangci-lint`(gosec 포함). 착수 전 딥리서치로 라이브러리 API 확정, 완료 후 4-차원 다중에이전트 어드버서리얼 리뷰로 9건 확정 결함 수정(admin 네트워크 오류→TransportError, Validator JWKS 타임아웃 주입, `exp` 필수, JWKS 재조회 rate-limit/single-flight 정합). Go는 레지스트리 없이 `go/v*` 태그가 곧 릴리스(human-gated). 최소 Go 1.25(x/oauth2 v0.36 요구). (2026-07-04)
- **(Node) `@xzawed/keycloak-sdk` 3번째 언어 SDK 추가 — Node.js/TypeScript(ESM·async-only).** 공식 `@keycloak/keycloak-admin-client` 26.6.4(Admin) + `openid-client` v6 6.8.4 함수형 API(인증) 래핑 + `jose` 5.10.0 자체 강화 JWT 검증. Java/Python과 §4 동형: `KeycloakClient`(auth 즉시·admin 지연·`close()`/`Symbol.asyncDispose`), `AuthClient`(PKCE·client-credentials·exchangeCode·refresh·introspect·logout·validate), `AdminClient`(users/clients/realms/roles/groups + `raw()` + 타임아웃 주입 + 예외 경계변환), `TokenSet`/`ValidatedToken`/`IntrospectionResult`·`Keycloak*Error` 계급·`TokenProvider`. JWT 강화(alg 핀·`none` 거부·iss 정확일치·aud 포함검사·클록 스큐·DoS-안전 JWKS). 단위 71 + Testcontainers 통합 5(실제 Keycloak 26.6) = **총 76 GREEN**, `tsc`(strict)·`eslint` 통과. 착수 전 딥리서치로 라이브러리 API 확정, 완료 후 4-차원 다중에이전트 어드버서리얼 리뷰로 7건 확정 결함 수정(HIGH: PKCE `exchangeCode` nonce 미전달로 id_token 거부; 값타입 동형성 `idToken`/`expiresAt`/`username`/`clientId` 보강; config 시크릿 마스킹; single-flight 경합). npm Trusted Publishing(OIDC + provenance) 릴리스 CI 준비(human-gated). (2026-07-04)
- **(Docs) Keycloak *서버* 배포 가이드 신설** — `docs/guides/deploying-keycloak-server.md`(단일 VM + Docker Compose 프로덕션: Keycloak 26.x + PostgreSQL + Caddy 자동 TLS, hostname/proxy-headers/health, 백업·업그레이드·SDK 연결). SDK는 클라이언트 라이브러리라 별도 Keycloak 서버가 필요하다는 흔한 혼란을 해소. getting-started·README에 링크. (2026-07-03)
- **(Docs) 설치/시작 가이드·언어 확장 로드맵·add-a-language 플레이북 신설 + README front door 재구성.** `docs/guides/getting-started.md`(언어별 요구 런타임·로컬/배포후 설치·최소 사용 예), `docs/roadmap/language-support.md`(depth-first 전략·step-0 실배포 체크리스트·우선순위 TS/Node→Go→C#→PHP→Rust→Ruby·현황 매트릭스), `docs/guides/add-a-language-playbook.md`(새 언어를 Java/Python 품질로 추가하는 6단계 표준 절차 + G1~G6 매핑). README는 상세 QuickStart를 시작 가이드로 이관하고 요약+딥링크만 남김. (2026-07-03)

### Changed
- **(Java·Kotlin) ⚠️ admin 부분 업데이트에서 `null`로 필드를 비울 수 없게 된다 — 공식 admin-client 동작으로의 복원 (PR #84·#85).** `resteasyClient(...)` 주입(타임아웃 하드닝)이 admin-client의 상류 `JacksonProvider` 등록을 통째로 우회해 `setSerializationInclusion(NON_NULL)`과 `configure(FAIL_ON_UNKNOWN_PROPERTIES, false)`를 둘 다 잃고 있던 **잠재 결함**을 정정했다. 그동안 우리는 설정하지 않은 필드까지 `null`로 전송했고, 서버가 우리 모델에 없는 필드를 반환하면 응답 역직렬화가 깨질 수 있었다. 이 결함은 admin-client **26.0.11**이 `UserRepresentation.verifiableCredentials`를 추가하며 드러났다 — 우리가 그 필드를 `null`로 실어 보내 서버 26.6이 400(*Unrecognized field*)을 냈고, 통합테스트 실패가 버전 비호환처럼 보였으나 실제로는 우리 쪽 문제였다. 이제 `buildTimeoutClient`가 `JacksonProvider`(우선순위 100)와 `StreamMessageBodyReader`(26.0.11에서 Stream 처리가 프로바이더에서 분리됨)를 직접 등록한다. **소비자 영향**: 미설정 필드는 전송되지 않아 서버가 '변경 없음'으로 처리하므로, 필드를 비우려면 빈 문자열이나 전용 API를 써야 한다. 회귀테스트 6건(Java 3 + Kotlin 3) + 부분 업데이트 보존 단언으로 잠갔다. (2026-07-22)
- **(Node) ⚠️ BREAKING — 지원 런타임 하한을 Node 20 → 22로 상향 (PR #87).** Node 20은 2026-04-30 EOL이고 DefinitelyTyped가 `types/node/v20`을 제거해 기존 `@types/node ^20` 핀 자체가 유지보수 종료 상태였다. `engines.node >= 22` · `@types/node ^22`(타입은 최신이 아니라 engines 하한을 따라간다) · CI 매트릭스 `['22','24']` · 하네스 이미지 `node:22-alpine` · 문서를 함께 옮겼다. 레지스트리 게시 0회 시점이라 소비자 비용 없음. (2026-07-22)
- **(Node) 의존성 메이저 전진 (PR #79·#80·#86).** `jose` **5.10.0 → 6.2.4**(`openid-client` 6.8.4가 이미 `jose ^6.2.2`를 요구해 트리가 **dedupe**된다. SDK가 쓰는 7개 API/옵션이 v6에서 이름·의미 동일함을 published `.d.ts`로 확인하고, JWKS `cooldownDuration` rate-limit이 살아있음을 히트 수로 실측) · `typescript` **5.9.3 → 6.0.3**(JS 기반 안정 라인. 산출 `dist/**`가 TS 5.9.3과 **바이트 동일**) · vitest 3.2.7 · prettier 3.9.6 · typescript-eslint 8.65.0. jose v6가 제거한 `KeyLike` import 정정 포함 — `tsconfig`의 `include`가 `["src"]`라 테스트가 타입체크되지 않아 typecheck·lint·테스트 어느 것도 잡지 못했던 사각이다. (2026-07-22)
- **(Rust) 의존성 메이저 전진 — `jsonwebtoken` 10.4.0 → 11.0.0 (PR #108).** 11.0.0의 "JWKs with unknown key types are now deserializable" 변경으로 **기형 JWKS의 거부 단계가 옮겨졌다** — 10.x는 RSA 변형에 맞지 않는 키(예: `n`이 JSON 배열)를 만나면 `JwkSet` 역직렬화가 통째로 실패해 `Transport`가 됐지만, 11.x는 그 키를 미지 키타입으로 역직렬화에 성공시키고 한 단계 뒤 `DecodingKey::from_jwk`가 거부해 `TokenValidation`이 된다. **거부 자체는 그대로여서 fail-closed는 유지**되고, 오히려 이 관용성이 상향의 근거다 — 10.x에서는 Keycloak이 realm에 우리가 모르는 kty(예: EdDSA) 키를 하나만 추가해도 같은 세트의 멀쩡한 RSA 키까지 못 써 **모든 토큰 검증이 죽는** 가용성 사고였다. 두 성질을 테스트로 고정했다(`rejects_jwks_with_n_as_array`가 새 오류 계급을, 신규 `tolerates_unknown_kty_alongside_valid_key`가 관용성을 — 로컬 실측 45개 전부 통과). 나머지 breaking change는 우리 표면에 닿지 않고 MSRV 1.88도 그대로다. 레지스트리 게시 0회 시점이라 소비자 비용 없음. (2026-07-31)
- **(CI) SonarCloud를 Dependabot PR에서 건너뛴다 (PR #83).** Dependabot이 트리거한 run에는 Actions 시크릿이 노출되지 않아(별도 Dependabot 시크릿 스토어가 비어 있음) `SONAR_TOKEN`이 빈 값으로 보간되며 스캔이 **반드시** 실패했다 — 코드 신호가 아니라 환경 산물. 토큰을 Dependabot 스토어에 복제하는 대안은 기각했다(이 잡은 PR head에서 `npm ci`·`composer install`·`mvn verify`를 실행하므로 방금 bump된 미검토 패키지 코드가 토큰과 같은 잡에서 돌게 된다). push 이벤트와 사람이 연 PR의 게이트는 불변. (2026-07-22)
- **(Java) ⚠️ BREAKING — 요구 런타임을 JDK 17 → 21 LTS로 상향.** `maven.compiler.release=21` + enforcer `requireJavaVersion=[21,)`. 아티팩트는 `--release 21`로 컴파일되므로 **소비자도 JDK 21+에서 실행**해야 하며, 이전 JDK에서는 `UnsupportedClassVersionError`가 발생합니다. `maven-compiler-plugin`을 `3.11.0`으로 명시 고정(기본값 드리프트 방지). CI·릴리스 워크플로도 JDK 21 단일 사용. 소스·공개 API 무변경. (2026-07-03)

- **(9개 언어) JWKS 재조회 최소 간격 기본값을 30초로 정렬했다(기존 10·30·60초 세 갈래).** 이 발산은 설계 결정이 아니라 PR #71에서 config화할 때 "기존 동작 무변경"을 위해 각 언어의 기존 하드코딩 값을 그대로 기본값으로 삼은 **산물**이었다 — 같은 위조 kid 폭주에 Ruby가 Python보다 IdP를 6배 자주 때렸다. 30초는 Nimbus `DEFAULT_RATE_LIMIT_MIN_INTERVAL`과 같은 값이라 외부 근거가 있는 유일한 후보이고, 60초 대비 키 로테이션 회복 창을 절반으로 줄인다. 레지스트리 게시 0회 시점이라 소비자 비용 없음.
- **(릴리스) `release-trigger.sh`가 이제 RC 태그를 만든다.** DEPLOY.md §7은 첫 태그를 RC로 하라고 권고하는데 헬퍼는 `X.Y.Z`만 받아 그 권고를 따를 수 없었다. 버전 검증을 언어별로 나눠 PEP 440(`0.1.0rc1`)·RubyGems(`0.1.0.rc1`)·Maven(`0.1.0-RC1`)·SemVer(`0.1.0-rc.1`) 표기를 각각 받고, 틀린 표기는 올바른 예시와 함께 거부한다 — 태그↔매니페스트 가드가 문자열 정확비교라 여기서 틀리면 CI가 막기 때문이다.
- **(릴리스) Java 릴리스도 발행 전 통합 게이트를 잡 경계에 명시한다.** 기존에는 단일 `release` 잡이 Maven lifecycle에 의존해 통합 테스트를 인라인으로 돌렸다 — 동작은 했지만 `-DskipITs` 한 줄로 조용히 사라질 수 있는 보장이었다. 이제 `release.yml`에 `integration` 잡이 있고 `release`가 이를 `needs:`로 잡아, 아홉 언어가 같은 방식으로 게이트를 서술한다.

### Security
- **(9개 언어) 광고만 하고 증명하지 않던 JWT 하드닝 불변식을 테스트로 닫았다.** README는 아홉 언어 전부 6개 불변식(alg 핀·iss 정확일치·aud 포함검사·`exp` 필수·클록 스큐·DoS-safe JWKS 재조회)을 보장한다고 말하는데, 감사 결과 **행동 테스트로 증명된 언어는 Rust·Ruby 둘뿐**이었다 — 나머지는 해당 방어 코드를 지워도 CI가 초록이었다. 채운 공백: Node `alg=none` 프로브 **부재**(테스트 전역 grep으로 확인) + HS/RS 혼동 · Java `exp` 필수/스큐 경계/JWKS 히트수 · .NET JWKS 히트수 + HS/RS 혼동 + 기형 JWKS · Kotlin JWKS 히트수 · PHP 스큐 경계 + HS/RS 혼동 · Python·Go 스큐 경계. 통과를 신뢰하지 않고 **변이검증**했다(방어 코드를 지웠을 때 실제로 깨지는지) — 그 결과 Node의 `alg=none`·혼동 프로브를 막는 것은 우리 alg 핀이 아니라 jose의 구조적 거부와 키 소스 분리임이 드러나 주석에 그대로 적었다. JWKS rate-limit 테스트에는 전부 **대조군**을 붙였다: 대조군이 없으면 캐시만으로도 "히트가 토큰 수보다 적다"가 성립해 `.rateLimited(...)`를 지워도 통과한다.
- **(Java·Kotlin) `jwksMinRefetch`가 Nimbus 캐시 TTL 이상일 때 하위 라이브러리 예외가 공개 API로 새던 것을 고쳤다.** `JWKSourceBuilder.build()`의 `IllegalStateException`이 `JwtValidator.forRealm`에서 그대로 올라와, 경계에서 SDK 타입으로 변환한다는 §4 계약을 위반했다. 지금은 두 언어 모두 `KeycloakConfigException`으로 변환하고 한계값을 메시지에 담는다. JWKS rate-limit 행동 테스트를 작성하다 발견했다.
- **(Java) jackson-databind 계열 6종 `2.21.2` → `2.21.4`.** Dependabot 경보 7건(전부 jackson-databind, HIGH 2·MEDIUM 5) 대응. CVE-2026-54512/54513/54514/54516/54517/54518 **6건 해소**. CVE-2026-54515는 fix(2.21.5) 미출시로 버전 해결 불가이나, 다중에이전트 적대적 트리아지로 **7건 전부 이 SDK 사용맥락에서 악용 불가**로 판정(전이 전용·default typing 없음·신뢰된 Keycloak 응답만 고정 POJO로 역직렬화·JWT는 Nimbus) → `not_used`로 dismiss + 2.21.5 상향 트래킹([이슈 #8](https://github.com/xzawed/KeyCloakSDK/issues/8)). `jackson-annotations`는 별도 트랙·비취약이라 `2.21` 유지. 전체 근거: [`docs/governance/verification-log.md`](docs/governance/verification-log.md). (2026-07-03)
- **(Java) CI 보안 불변식 가드 추가.** SDK 소스가 Jackson default/polymorphic typing 활성화나 커스텀 JAX-RS Jackson provider 등록을 도입하지 못하도록 CI(`ci.yml` `invariant` 잡)에서 소스 스캔으로 강제 — 위 jackson-databind CVE 무위험 판정의 전제(불변식)를 회귀로부터 보호. (2026-07-03)

---

<!-- 릴리스 시: [Unreleased] 아래에 `## [x.y.z] - YYYY-MM-DD` 섹션을 만들고 해당 항목을 이동한다.
     태그: Java `v*` · Python `py-v*` · Node `node-v*` · Go `go/v*` · C#/.NET `dotnet-v*` · PHP `php-v*` · Rust `rust-v*` · Ruby `ruby-v*`. -->
