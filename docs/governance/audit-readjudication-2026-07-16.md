# 감사 미판정 findings 재판정 로그 (2026-07-16)

의 미판정 24건(refuted_unadjudicated 15 + verify_failed_unadjudicated 9)을 배포 전 하드닝(PR1~8) + 경계 리뷰(#67/#69/#70) + JWKS config(PR#71) **완료 후 현재 main 코드**에 대해 24-에이전트 병렬 워크플로로 재판정했다.

**결과: still-real 결함 0건.** correctly-refuted 13 · fixed-later 10 · design-intent(외부 제약) 1.

| # | lang | 판정 | 근거 |
|---|---|---|---|
| 1 | java | correctly-refuted | java/pom.xml:104-110 (AuthClient.class excluded with documented rationale at line 107: "라이브 네트워크 경계: 단위테스트 불가, Phase 6 통합테스트로 검증"). Error-mapping branches at java/keycloak-sdk-auth/.../AuthClient.java:102-106,159-163,182-186,198-200,230-233 are all standard co |
| 2 | python | fixed-later | python/src/keycloak_sdk/admin/__init__.py:53 and python/src/keycloak_sdk/aio/admin/__init__.py:58 now pass `timeout=self._config.read_timeout` (raw float, `# type: ignore[arg-type]`). Fixed by commit c0bbf70 "fix(python): admin 타임아웃이 sub-second read_timeout을 0 |
| 3 | node | fixed-later | node/src/config.ts:24,43,76 (signatureAlgorithms config field, default ['RS256'], overridable, empty rejected at :60-62); node/src/auth.ts:61 (allowedAlgs: [...cfg.signatureAlgorithms] — reads config, not hardcoded); node/src/jwt.ts:39 (jwtVerify algorithms: t |
| 4 | node | correctly-refuted | node/src/admin/users.ts:31 (async search(username?, first = 0, max = 100)) and node/src/admin/groups.ts:31 (async list(first = 0, max = 100)). `first`/`max` are caller-overridable DEFAULT parameters, not hardcoded literals, documented in JSDoc as "페이지네이션 first |
| 5 | node | correctly-refuted | node/src/jwt.ts:38-44 — jwtVerify pins algorithms to allowedAlgs (default RS256) and jose structurally rejects alg:none; tampered signatures fail crypto verification and are caught→rethrown as KeycloakTokenValidationError (line 54-55). node/test/unit/jwt.test. |
| 6 | go | correctly-refuted | go/auth.go:42-47 scope() returns strings.Join(cfg.Scopes, " ") when Scopes is non-empty (correct OAuth2 RFC 6749 §3.3 space-delimited format) else "openid". go/auth.go:69 SetAuthURLParam("scope", a.scope()) uses oauth2 setParam -> url.Values.Set (override, not |
| 7 | dotnet | correctly-refuted | dotnet/src/Xzawed.Keycloak.Sdk/JwtValidator.cs:60-65 — ValidateAsync accepts `ct` but calls Handler.ValidateTokenAsync(token, _tvp) without it. Verified against current code + csproj (Microsoft.IdentityModel.JsonWebTokens 8.19.2, JwtValidator.cs:1, csproj:14): |
| 8 | dotnet | fixed-later | dotnet/src/Xzawed.Keycloak.Sdk/AuthClient.cs:95-104 is now fail-closed — `if (nonce is not null)` then `if (tokens.IdToken is not { } idToken) throw new KeycloakAuthException("id_token missing for nonce validation")`, followed by full validator.ValidateAsync o |
| 9 | dotnet | correctly-refuted | dotnet/src/Xzawed.Keycloak.Sdk/KeycloakClient.cs:40 (Audiences = new[] { cfg.ClientId }) + JwtValidator.cs:55-56 (ValidateAudience=true, ValidAudiences membership check, "multi-aud safe"). Isomorphic across langs: node/src/jwt.ts:41, python/src/keycloak_sdk/jw |
| 10 | dotnet | correctly-refuted | dotnet/src/Xzawed.Keycloak.Sdk/Admin/UsersResource.cs:17 — SearchAsync(string? username, int first = 0, int max = 100, ...) forwards first/max to GetUsersRequestParameters. `max=100` is an explicit, caller-overridable pagination default (caller paginates via ` |
| 11 | dotnet | correctly-refuted | CLAUDE.md:138 documents the coverage-gate exclude of [*]Xzawed.Keycloak.AuthClient/Admin.*/KeycloakClient as network-boundary classes verified via the Testcontainers E2E Full_flow integration test (dotnet/tests/.../integration). AuthClient logic branches ARE u |
| 12 | rust | correctly-refuted | rust/src/jwks.rs:51-90 (get_key). Cold cache + unknown kid does produce 2 fetches: fetch#1 at line 61-62 is the bootstrap load (exempt, not gate-stamped, documented line 52 "초기 로드 — rate-limit 예산 미소모"), fetch#2 at line 85-86 is the single gated refetch (gate s |
| 13 | rust | correctly-refuted | rust/src/client.rs:29 production path now passes config.jwks_min_refetch_secs to JwksStore::new (was the hardcoded 60). config.rs:20/53/77 add the field (default 60) + with_jwks_min_refetch_secs builder; config.rs:106-113 tests default+custom. Remaining litera |
| 14 | kotlin | fixed-later | Create path was never a defect: CreatedResponseUtil.getCreatedId (keycloak-admin-client 26.0.10 source, verified from sources jar) reads the JSON error body via readEntity(Map.class) and embeds the server errorMessage/error into the WebApplicationException mes |
| 15 | harness | fixed-later | Was real (silent exit 0 on lang failure), now fixed. harness/verify.sh:56-61 accumulates FAILED_LANGS across build/healthz/conformance/security/suite and `exit 1` if non-empty; harness/install/install-verify.sh:909-912 runs install-matrix.mjs --strict → MATRIX |
| 16 | java | fixed-later | java/keycloak-sdk-admin/src/main/java/io/github/xzawed/keycloak/admin/AdminExceptions.java:20-24 now catches ProcessingException and throws KeycloakTransportException. Fixed by commit 078122e ("fix(java): admin 전송 오류(ProcessingException)를 KeycloakTransportExce |
| 17 | node | fixed-later | node/src/admin/call.ts:21-23 now converts transport failures to KeycloakTransportError via isTransportError() (node/src/transport.ts:30-42). Fixed by commit 63d1f9c "fix(node): admin 전송 오류를 KeycloakTransportError로 변환 (raw 누출 차단)", merged via PR #37 (fix/transp |
| 18 | node | fixed-later | node/src/jwt.ts:43 now sets requiredClaims:['exp'] (rejects exp-less tokens); node/test/unit/jwt.test.ts:86-98 adds test 'exp 클레임이 없는 토큰 → 거부'. Both added by commit ca4ecc5 (PR1 #35, 2026-07-14) — node was the only lang that previously allowed missing exp; TDD |
| 19 | node | fixed-later | node/src/admin/call.ts:22-25 now converts transport errors (isTransportError→KeycloakTransportError) before the fallthrough `throw err`; node/test/unit/admin.test.ts:131-152 split the old blanket "stateless error propagated as-is" test into (a) line 131 cause- |
| 20 | php | fixed-later | php/src/KeycloakConfig.php:38 (added `jwksMinRefetchSeconds = 60`, validated >=0 at lines 53-55) + php/src/KeycloakClient.php:36 (`new JwksStore(..., $config->jwksMinRefetchSeconds)`) wire config into JwksStore. Fixed by commit d5b7dd5 "feat: JWKS 재조회 최소 간격을 9 |
| 21 | kotlin | design-intent | kotlin/src/integrationTest/kotlin/io/github/xzawed/keycloak/FullFlowIT.kt:174-190 (realms().get success + realms().create 403-Forbidden path exercised via SDK), :201-217 (realm create/delete SUCCESS via raw master container.keycloakAdminClient — documented §4  |
| 22 | kotlin | correctly-refuted | kotlin/src/main/kotlin/io/github/xzawed/keycloak/jwt.kt:97-101 — production forRealm() builds a real JWKSourceBuilder.create(jwksUrl, retriever).rateLimited(config.jwksMinRefetch).build() (Nimbus default cache+rateLimited = DoS-safe), NOT a static ImmutableJWK |
| 23 | kotlin | correctly-refuted | kotlin/src/main/kotlin/io/github/xzawed/keycloak/auth.kt:152-155 (introspect failure → throws KeycloakAuthException with err.code), :276-281 (clientAuth null → throws "Confidential client required: clientSecret is not configured"; KDoc notes this replaces Java |
| 24 | harness | correctly-refuted | harness/install/lib.sh:75-95 (wait_healthy polls `curl -fsS` only; on timeout returns 1 — the CORRECT failure verdict, merely slow, no wrong output/exception/leak). harness/install/install-verify.sh:653 (rust 2400s timeout) + caller checks `installed.ok` marke |

## 판정 요약

- **correctly-refuted (13)**: 애초에 구체 결함이 아님 — 커버리지-갭 메타논증·라이브러리 API 구조적 한계·페이지네이션 계약·audience 바인딩 설계·rate-limit 부트스트랩 동형성.
- **fixed-later (10)**: 감사 시점엔 실재했으나 후속 PR로 수정됨(각 항목에 수정 커밋 명시).
- **design-intent (1)**: #21 Kotlin realm-CRUD는 master-admin 권한 필요 — SDK의 client-credentials 서비스계정으로는 불가한 Keycloak 내부 제약(제품 결함 아님).
