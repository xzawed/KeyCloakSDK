# fenced fixture

This document explains the anchor syntax itself inside a fenced code block.
It must NOT be parsed as a real anchor (the referenced source does not even
exist — that is the point).

```
<!-- doc-guard: kind=dep source=fenced/does-not-exist.gradle.kts min=1 -->
```

No real anchors exist in this document.
