# npm lock fixture

`package.json` only states a declared range (`^3.0.0`). Docs that claim a
resolved version must point `source=` at `package-lock.json` instead.

<!-- doc-guard: kind=dep source=npm-lock/package-lock.json min=1 -->

| 이름 | 좌표 | 버전 |
|---|---|---|
| Widget | `widget` | 3.2.1 |
